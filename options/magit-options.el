;;; magit-options.el --- Magit / ediff options -*- lexical-binding: t; -*-

;;; Commentary:
;; Option settings for magit and ediff.

;;; Code:

(setq ediff-split-window-function 'split-window-horizontally)

(provide 'magit-options)
;;; magit-options.el ends here
